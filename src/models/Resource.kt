package models

// The class called "Resource" is a framework for the resource that we read from the "input.txt" file.
// At the moment our resource has nothing special besides its position but
// in the future we can add special functionality to our resource when its needed.
class Resource(var position: Int = 0)